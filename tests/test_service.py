from lib2to3.pgen2 import token
from service import service

def test_get_farmers(client):
    # farmer = service.get_farmer(client, "test")
    # assert farmer.farmer_name == "test"
    assert 2 == 2
